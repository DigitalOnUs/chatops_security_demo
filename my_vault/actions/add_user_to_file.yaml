---
name: "add_user_to_file"
runner_type: "python-script"
description: "Pasrt of adding user to authenticated group."
enabled: true
entry_point: "add_user_to_file.py"
parameters:
    secret_list:
        type: "object"
        description: "vault users list"
        required: false
    new_user:
        type: "string"
        description: "User to add"
        required: false
        #default: "{{action_context.parent.api_user}}"
    secret_file:
        type: "string"
        description: "secret file"
        required: false
